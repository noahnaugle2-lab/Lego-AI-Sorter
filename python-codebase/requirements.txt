# LEGO AI Sorting Machine — Python Dependencies
# Install with: pip install -r requirements.txt --break-system-packages

# Core
PyYAML>=6.0
requests>=2.28
flask>=3.0

# Camera & image processing (Pi 5)
opencv-python-headless>=4.8
numpy>=1.24

# Testing
pytest>=7.0

# Hardware control (install on Pi only)
# RPi.GPIO>=0.7            # Uncomment on Raspberry Pi
# adafruit-circuitpython-pca9685>=3.4  # Uncomment on Raspberry Pi
# adafruit-circuitpython-motor>=3.4    # Uncomment on Raspberry Pi

# Optional: Rebrickable integration
# (requests already covers this)
